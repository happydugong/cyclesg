name: Refresh Google My Maps Data

on:
  workflow_dispatch:
    inputs:
      overlay_id:
        description: Required. Provide exactly one Google My Maps overlay id.
        required: true
        type: string

permissions:
  contents: write

jobs:
  refresh-google-my-maps-data:
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 9

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Fetch Google My Maps raw data
        env:
          OVERLAY_SOURCE_ID: ${{ inputs.overlay_id }}
        run: pnpm overlay:fetch:my-maps

      - name: Convert Google My Maps overlays
        env:
          OVERLAY_SOURCE_ID: ${{ inputs.overlay_id }}
        run: pnpm overlay:convert:my-maps

      - name: Commit and push changes
        run: |
          if git diff --quiet -- "data/${{ inputs.overlay_id }}"; then
            echo "No Google My Maps data changes detected."
            exit 0
          fi

          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add "data/${{ inputs.overlay_id }}"
          git commit -m "chore: refresh google my maps data"
          git push
